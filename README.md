# MiniMQTT (C)

This is a network programming project - basic implementation of MQTT in C.

## 🛠 Requirements
- C compiler (gcc/clang)
- CMake ≥ 3.16
- Make

## ▶️ Build & Run

```sh
mkdir build
cd build
cmake -S . -B build
cmake --build build
./build/MiniMQTT
./build/client_tcp

